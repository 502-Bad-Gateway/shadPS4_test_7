# SPDX-FileCopyrightText: 2024 shadPS4 Emulator Project
# SPDX-License-Identifier: GPL-2.0-or-later

name: Build and Release

# The generic upstream multi-OS CI is intentionally disabled on this experimental
# Windows 7 dumb-GPU branch. Use the dedicated Win7 dumb-GPU workflow instead.
on:
  workflow_dispatch:

jobs:
  disabled-on-win7-dumb-gpu:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Generic Build and Release is disabled on win7-v1-dumb-gpu."
